apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cycling-blog-app-service
  namespace: jacksapp-dev
spec:
  selector:
    matchLabels:
      app: cycling-blog-app-service
  endpoints:
  - port: tcp # this is the port name you grabbed from your running service
    interval: 15s
