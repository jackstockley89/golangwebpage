Commands used to run helm chart

run helm --dry-run
```
helm install cycling-blog cycling-blog/ --values cycling-blog/values.yaml --dry-run
```

run helm
```
helm install cycling-blog cycling-blog/ --values cycling-blog/values.yaml
```